# linear-regression-model-
Supervised linear regression on the scikit-learn diabetes dataset. A single feature (BMI) is used to predict quantitative disease progression. The notebook includes data inspection, train/test partitioning, model fitting, mean squared error evaluation, coefficient inspection, and visualization of predictions against observed values.
